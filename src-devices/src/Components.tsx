import RtspCameraComponent from './RtspCameraComponent';
import SnapshotCameraComponent from './SnapshotCameraComponent';

// The keys must match `common.deviceWidgets.components[].name` in io-package.json
export default {
    RtspCameraComponent,
    SnapshotCameraComponent,
};
